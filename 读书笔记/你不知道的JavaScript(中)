### 第二章 值

##### 2.3 数字

```a | 0```操作会将a变量中的数值转成32位有符号整数，因为数位运算符``` | ```只适合于32位整数（它只关心32位，其余位将被忽略）；而如```NaN```和```Infinity```，对它们``` | ```会执行虚拟操作```ToInt32```，以便转换成符合数位运算符要求的+0值 
```javascript
	var a = 23333;
	console.log(a | 0);        // 23333
	console.log(NaN | 0);      // 0
	console.log(Infinity | 0); //0